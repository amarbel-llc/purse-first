# compression_type — deferred

This package was deferred from the initial dodder lib fork because it depends on
`github.com/DataDog/zstd`, which requires CGo. Add it when CGo support is
configured in the Nix build.

Source: `code.linenisgreat.com/dodder/go/lib/delta/compression_type`
